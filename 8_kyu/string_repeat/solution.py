def repeat_str(repeat, string):
    repeating_string = "" # setting things up
    for x in range(repeat): # 0 to repeat number -1 - concatenates the repeating string repeat number of times
        repeating_string += string 
    return repeating_string
